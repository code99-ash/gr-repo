import { GlobalExceptionsFilter } from './global.filter';

describe('GlobalExceptionsFilter', () => {
  it('should be defined', () => {
    expect(new GlobalExceptionsFilter({} as any)).toBeDefined();
  });
});
